import { type Metadata } from "next"
import { type ReactNode } from "react"

export const metadata: Metadata = {
    title: "SFB - Доставка",
}

export default function LayoutDelivery({ children }: { children: ReactNode }) {
    return children
}
